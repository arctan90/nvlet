# create env
```
# create conda env
conda create -n nvlet python=3.11
# activate it
conda activate  nvlet
# install torch
pip install torch
# or on mac pip3 install --pre torch torchaudio --index-url https://download.pytorch.org/whl/nightly/cpu
```